
<h1> User info</h1>

　

         <%= attachment_image_tag user, :profile_image, format: 'jpeg', size: "100x100", fallback: "no_image.jpg" %>
          <p>name</p>
          <%= user.name %>
          <p>introduction</p>
          <%= user.introduction %>

          <%= link_to '',edit_user_path(user.id) , class: "glyphicon glyphicon-star" %>


<h1>New book</h1>
<%= form_for(book) do |f| %>
<% if book.errors.any? %>
        <%= book.errors.count %>error prohibited this book from being saved:
        <% book.errors.full_messages.each do |message| %>
            <%= message %>
        <% end %>
    <% end %>
    <h4>title</h4>
    <%= f.text_field :title %>
    <h4>Optipn</h4>
    <%= f.text_area :body %>
    <%= f.submit 'Create Book' %>
<% end %>

